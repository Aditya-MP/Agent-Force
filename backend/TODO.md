# TODO: Fix GoogleAI Constructor Error

- [ ] Update backend/package.json to replace '@google/genai' with '@google/generative-ai'
- [ ] Update backend/index.js to import 'GoogleGenerativeAI' instead of 'GoogleAI' and adjust instantiation
- [ ] Run 'npm install' in backend directory to install the correct package
- [ ] Test by running 'npm run dev' to ensure the server starts without errors
